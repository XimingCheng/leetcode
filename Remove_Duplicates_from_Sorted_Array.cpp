// class Solution {// public:    // int removeDuplicates(int A[], int n) {        // // IMPORTANT: Please reset any member data you declared, as        // // the same Solution instance will be reused for each test case.        // if(n == 0) return 0;        // int curVal = A[0];        // for(int i = 1; i < n; i++) {            // if(A[i] == curVal) {                // for(int j = i + 1; j < n; j++)                    // A[j - 1] = A[j];                // n--;                // i--;            // } else {                // curVal = A[i];            // }        // }        // return n;    // }// };class Solution {public:    int removeDuplicates(int A[], int n) {        if (n == 0) return 0;        else if (n == 1) return 1;        int idx1 = 0, idx2 = 1;        for (; idx2 < n; idx2++)        {            if (A[idx2] == A[idx1]) continue;            A[++idx1] = A[idx2];            for (int j = idx2 + 1; j < n; j++)            {                if (A[j] == A[idx1]) continue;                else                {                    idx2 = j - 1;                    break;                }            }        }        return idx1 + 1;    }};